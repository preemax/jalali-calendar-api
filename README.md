📅 Jalali Calendar API (Free & Open Source)این پروژه یک سرویس API خودکار و رایگان برای دریافت مناسبت‌های تقویم هجری شمسی است. داده‌ها به صورت روزانه از مراجع معتبر استخراج شده، توسط هوش مصنوعی به انگلیسی ترجمه شده و در قالب فایل‌های JSON سبک منتشر می‌شوند.🌟 ویژگی‌های برجستهترجمه هوشمند: تمام مناسبت‌ها به انگلیسی ترجمه شده‌اند تا در نمایشگرهای TFT/OLED بدون نیاز به فونت فارسی نمایش داده شوند.پوشش کامل سال: شامل ۱۲ فایل مجزا برای تمام ماه‌های سال (۱ تا ۱۲).تشخیص تعطیلات: روزهای تعطیل رسمی با رنگ قرمز (یا فلگ اختصاصی) مشخص می‌شوند.پایداری بالا: میزبانی روی GitHub Raw با سرعت دسترسی عالی.📡 نحوه استفاده (API Endpoints)برای دریافت اطلاعات هر ماه، از آدرس زیر استفاده کنید (شماره ماه را در آخر آدرس تغییر دهید):https://raw.githubusercontent.com/preemax/jalali-calendar-api/main/[MONTH].jsonمثال‌ها:📍 فروردین: .../main/1.json📍 دی: .../main/10.json📊 ساختار فایل JSONکلیدنوعتوضیحاتdعددشماره روز ماه (۱ تا ۳۱)tمتنشرح مناسبت به انگلیسیhBooleanوضعیت تعطیلی (true = تعطیل)🛠 نمونه کد برای ESP32 (Arduino)C++void fetchMonth(int monthNumber) {
    HTTPClient http;
    String url = "https://raw.githubusercontent.com/preemax/jalali-calendar-api/main/" + String(monthNumber) + ".json";
    http.begin(url);
    if (http.GET() == 200) {
        String payload = http.getString();
        // دیتای دریافتی را با ArduinoJson تحلیل کنید
    }
    http.end();
}
Developed with ❤️ for the Iranian Open Source Community
