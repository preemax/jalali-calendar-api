# 📅 Jalali Calendar Free API

یک **API رایگان و خودکار** برای دریافت مناسبت‌ها و تعطیلات تقویم **هجری شمسی (جلالی)** ایران.

این سرویس، داده‌های مناسبتی را از سایت `time.ir` جمع‌آوری می‌کند، به انگلیسی ترجمه می‌کند و در قالب فایل‌های JSON سبک منتشر می‌نماید تا در پروژه‌های نرم‌افزاری و سخت‌افزاری قابل استفاده باشند.

🔗 لینک مخزن: https://github.com/preemax/jalali-calendar-api

---

## 🌟 ویژگی‌ها

- 🧠 **ترجمه انگلیسی خودکار**  
  همه مناسبت‌ها به انگلیسی ترجمه شده‌اند تا در نمایشگرهای کاراکتری (OLED/TFT) بدون نیاز به فونت فارسی قابل‌نمایش باشند.

- 📆 **پوشش کامل سال شمسی**  
  شامل ۱۲ فایل JSON مجزا (هر فایل مخصوص یک ماه).

- 🚩 **تفکیک تعطیلات رسمی**  
  روزهای تعطیل با فلگ مشخص شده‌اند تا در UI با رنگ/آیکن متمایز نمایش داده شوند.

- 🚀 **بدون نیاز به سرور**  
  فایل‌ها روی GitHub میزبانی شده‌اند و برای استفاده نیاز به سرویس جداگانه ندارند — رایگان و پایدار.

- 🔄 **به‌روزرسانی خودکار**  
  با استفاده از **GitHub Actions** داده‌ها دسته‌بندی، ترجمه و منتشر می‌شوند.

---

## 📡 نحوه استفاده (Endpoints)

برای دریافت اطلاعات مناسبتی هر ماه، به آدرس زیر مراجعه کنید و شماره ماه (1 تا 12) را جایگزین کنید:



https://raw.githubusercontent.com/preemax/jalali-calendar-api/main/[MONTH].json


### 📍 نمونه لینک‌ها



https://raw.githubusercontent.com/preemax/jalali-calendar-api/main/1.json
 ← Farvardin
https://raw.githubusercontent.com/preemax/jalali-calendar-api/main/4.json
 ← Tir
https://raw.githubusercontent.com/preemax/jalali-calendar-api/main/10.json
 ← Dey
https://raw.githubusercontent.com/preemax/jalali-calendar-api/main/12.json
 ← Esfand


---

## 📊 JSON Schema (ساختار داده‌ها)

خروجی هر ماه به صورت یک JSON شیء ساده و بهینه برای استفاده در سیستم‌های کم‌حافظه است:

```json
{
  "year": 1404,
  "month": 10,
  "events": [
    {
      "d": 11,
      "t": "Birthday of Imam Ali (AS) and Father's Day",
      "h": true
    },
    {
      "d": 19,
      "t": "Firefighting Day",
      "h": false
    }
  ]
}
```

📌 توضیح فیلدها
کلید	نوع	توضیح
d	Integer	شماره روز ماه (1 تا 31)
t	String	عنوان مناسبتی به انگلیسی (ترجمه)
h	Boolean	تعطیل رسمی بودن (true = تعطیل)
🛠 راهنمای استفاده در ESP32 / Arduino

در پروژه‌های سخت‌افزاری مثل ESP32 / ESP8266 / Arduino می‌توانید از کد نمونه زیر استفاده کنید:

#include <HTTPClient.h>
#include <ArduinoJson.h>

void fetchCalendarData(int month) {
    HTTPClient http;
    String url = "https://raw.githubusercontent.com/preemax/jalali-calendar-api/main/" 
                 + String(month) + ".json";

    http.begin(url);
    int status = http.GET();
    if (status == 200) {
        String payload = http.getString();

        DynamicJsonDocument doc(2048);
        deserializeJson(doc, payload);

        // پردازش داده‌ها (مثلاً نشان دادن در OLED/TFT)
        for (auto &event : doc["events"].as<JsonArray>()) {
            Serial.println(event["t"].as<String>());
        }
    }
    http.end();
}


📌 مناسب برای:

ESP32 / ESP8266

Arduino

پروژه‌های IoT و نمایشگرهای کوچک

🤝 مشارکت و توسعه

اگر پیشنهادی برای:

بهبود ترجمه‌ها

افزودن توضیحات مناسبت‌ها

گسترش API

دارید، خوشحال می‌شویم مشارکت کنید:

این مخزن را Fork کنید

تغییرات خود را اعمال کنید

Pull Request ارسال کنید

📜 لایسنس

این پروژه برای استفاده شخصی و تجاری کاملاً رایگان است و می‌توانید آزادانه در پروژه‌هایتان از آن بهره ببرید.

🇮🇷 توسعه داده شده با ❤️ برای جامعه متن‌باز ایران
